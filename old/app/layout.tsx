import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Egie Sugina - Full-Stack Developer & Cybersecurity Enthusiast",
  description: "Hacker-themed portfolio showcasing full-stack development skills, cybersecurity expertise, and innovative projects.",
  keywords: ["Full-Stack Developer", "Cybersecurity", "React", "Node.js", "Python", "Portfolio"],
  authors: [{ name: "Egie Sugina" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
